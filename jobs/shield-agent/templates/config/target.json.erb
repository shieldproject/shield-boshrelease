<%
  my_ip = spec.networks.send(spec.networks.methods(false).first).ip
%>
<% if !properties.shield.target.name.nil? %>
{
  "name":     "<%= p("shield.target.name") %>",
  "plugin":   "<%= p("shield.target.plugin") %>",
  "endpoint": <%= p("shield.target.config").to_json.dump %>,
  "agent": "<%= my_ip %>:<%= p("shield.agent.port") %>"
}
<% end %>