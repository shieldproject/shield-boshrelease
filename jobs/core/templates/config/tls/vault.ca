<%
x = p("vault.tls.ca-certificate")
if !x.match('CERTIFICATE')
  x = x.gsub(/^(.{40}).*/, '\1...')
  raise "vault.tls.ca-certificate '#{x}' does not look like a TLS certificate"
end
%><%= p('vault.tls.ca-certificate') %>
