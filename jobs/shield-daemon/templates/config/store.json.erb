<%
  require 'json'
%>
<% if_p("shield.store.name", "shield.store.plugin", "shield.store.config") do |name, plugin, config| %>
{
  "name":     "<%= name %>",
  "plugin":   "<%= plugin %>",
  "endpoint": <%= config.to_json.dump %>
}
<% end %>
