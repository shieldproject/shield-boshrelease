---
name: import
packages:
  - shield

templates:
  # shield import
  bin/run:           bin/run
  config/import.yml: config/import.yml

properties:
  import:
    description: A complete `shield import' YAML file, for importing and auto-provisioning tenants, users, cloud storage, data systems, etc.

  username:
    description: shield username
    default: admin
  password:
    description: shield password
    default: shield
  domain:
    description: domain name for shield api
  core:
    description: shield core
    default: shield
