# abort script on any command that exits with a non zero value
set -e

tar xzf mysql/mysql-*.tar.gz
(
  set -e
  cd mysql-*/
  mkdir bld
  cd bld
  cmake .. -DWITHOUT_SERVER=ON -DWITH_EMBEDDED_SERVER=OFF -DCMAKE_INSTALL_PREFIX=${BOSH_INSTALL_TARGET}
  make
  make install
)
